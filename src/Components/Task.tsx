import React from 'react';

const Task = () => {

    return (
        <div>
            <input type="checkbox"/>
            <span>first tasck</span>
            <button style={{marginLeft:10}}>del</button>
        </div>
    );
};

export default Task;